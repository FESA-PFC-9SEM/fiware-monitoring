services:
  # MongoDB HISTÓRICO (com autenticação) - usado pelo STH Comet
  mongo-db-historical:
    image: mongo:4.4
    container_name: fiware-mongo-historical
    restart: no
    hostname: mongo-db-historical
    environment:
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=admin123
    command: ["mongod", "--nojournal", "--auth"]
    ports:
      - "27017:27017"  # <-- Exposto externamente, se necessário
    volumes:
      - mongo-historical-data:/data/db

  # MongoDB INTERNO (sem autenticação) - usado pelo Orion e IoT Agent
  mongo-db-internal:
    image: mongo:4.4
    container_name: fiware-mongo-internal
    restart: no
    hostname: mongo-db-internal
    command: ["mongod", "--nojournal", "--logpath", "/var/log/mongodb/mongodb.log"]
    networks:
      - backend
    # Nenhuma porta mapeada para o host
    volumes:
      - mongo-internal-data:/data/db
      - mongo-logs:/var/log/mongodb

  # Orion – Context Broker (usando mongo-db-internal, sem auth)
  orion:
    image: fiware/orion:3.11.0
    restart: no
    container_name: fiware-orion
    hostname: orion
    depends_on:
      - mongo-db-historical
    ports:
      - "1026:1026"
    command: >
      -dbhost mongo-db-historical
      -dbuser admin
      -dbpwd admin123
      -dbAuthDb admin
      -corsOrigin __ALL
      -corsMaxAge 600

  orion-exporter:
    image: fiware/service.orionexporter
    restart: no
    container_name: fiware-orion-exporter
    depends_on: 
      - orion
    ports:
      - "1027:"
    command: ["--config"]

  # STH Comet – Armazena dados históricos (usando mongo-db-historical, com auth)
  fiware-sth-comet:
    image: telefonicaiot/fiware-sth-comet
    restart: no
    container_name: fiware-sth-comet
    hostname: sth-comet
    depends_on:
      - mongo-db-historical
    ports:
      - "8666:8666"
    environment:
      - STH_HOST=0.0.0.0
      - STH_PORT=8666
      - DB_PREFIX=sth_
      - DB_URI=mongo-db-historical:27017  # <-- Aponte para o serviço renomeado
      - DB_USERNAME=admin
      - DB_PASSWORD=admin123
      - DB_AUTH_SOURCE=admin
      - LOGOPS_LEVEL=DEBUG

  # MQTT Broker – Mosquitto
  mosquitto:
    image: eclipse-mosquitto:latest
    restart: no
    container_name: fiware-mosquitto
    hostname: mosquitto
    ports:
      - "1883:1883"
      - "9001:9001"
    volumes:
      - ./mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf

  mosquitto-exporter:
    image: sapcc/mosquitto-exporter
    restart: no
    container_name: fiware-mosquitto-exporter
    ports: 
      - "9234:9234"
    command: ["--endpoint", "tcp://mosquitto:1883"]

  # IoT Agent – UL (usando mongo-db-internal, sem auth)
  iot-agent:
    image: fiware/iotagent-ul:latest
    restart: no
    container_name: fiware-iot-agent
    hostname: iot-agent
    depends_on:
      - mongo-db-internal
      - mosquitto
    expose:
      - "4041"
    ports:
      - "4041:4041"
    environment:
      # --- Conexão ao Orion ---
      - IOTA_CB_HOST=orion
      - IOTA_CB_PORT=1026
      - IOTA_CB_NGSI_VERSION=v2

      # --- Porta do IoT Agent ---
      - IOTA_NORTH_PORT=4041

      # --- Registro no Mongo (sem auth) ---
      - IOTA_MONGO_HOST=mongo-db-internal
      - IOTA_MONGO_PORT=27017

      # --- MQTT ---
      - IOTA_MQTT_HOST=mosquitto
      - IOTA_MQTT_PORT=1883

      # --- Outros ajustes ---
      - IOTA_LOG_LEVEL=DEBUG
      - IOTA_TIMESTAMP=true
      - IOTA_AUTOCAST=true
      - IOTA_PROVIDER_URL=http://iot-agent:4041

networks:
  backend:
    name: fiware-backend

volumes:
  mongo-historical-data:
  mongo-logs:
  mongo-internal-data:
